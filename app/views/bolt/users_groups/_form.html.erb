<%= form_for(@bolt_users_group) do |f| %>
  <% if @bolt_users_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bolt_users_group.errors.count, "error") %> prohibited this bolt_users_group from being saved:</h2>

      <ul>
      <% @bolt_users_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br />
    <%= f.number_field :group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
