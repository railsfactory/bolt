
<h2>Banners</h2>

<%= render :partial => 'bolt/banners/table', :locals => { :banners => @banners } %>

<%= will_paginate @banners %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{bolt_show_icon "add"}Add a new banner".html_safe, new_bolt_banner_path %></li>
<% end %>
