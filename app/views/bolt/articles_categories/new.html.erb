

<h2>Add a new articles category</h2>

<%= form_for @articles_category, :url => bolt_articles_categories_path do |f| %>
	
	<%= render :partial => 'bolt/articles_categories/form', :locals => { :f => f } %>
	
	<p class="submits">
		<%= link_to 'Cancel', bolt_articles_categories_path, :class => 'cancel' %> or
		<%= f.submit "Add new articles category", :class => "update" %>
	</p>

<% end %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{bolt_show_icon "table"}Back to list".html_safe, bolt_articles_categories_path %></li>
<% end %>
